{{#each error}}
  <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}
<h3>New Category:</h3>
<div class="card" id="cat">
  <div class="card-body">
    <form action="/admin/category/new" method="POST">
      <label for="name" class="mt-2">Name: </label>
        <input type="text" id="name" name="name" placeholder="Category name" class="form-control"> <br>
      <label for="slug" class="">Slug: </label>
        <input type="text" id="slug" name="slug" placeholder="Category slug" class="form-control"> <br>
      <a href="/admin/category/new"><button type="submit" class="btn btn-primary"> Add</button></a>
      
    </form>
  </div>
</div>